#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_minimize@1.8.1@minimize/bin/minimize" "$@"
  ret=$?
else 
  node  "$basedir/../../../_minimize@1.8.1@minimize/bin/minimize" "$@"
  ret=$?
fi
exit $ret
